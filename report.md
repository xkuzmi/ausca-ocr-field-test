# Ausca Document OCR end-to-end field report

Run window: 2026-09-19T21:23:56.096Z to 2026-09-19T21:24:17.844Z. Frantic claim: `87261269-8e9a-404c-961e-f7c718168cc2`.

- **Discovery — 174.631 ms.** I fetched the per-offer [SKILL.md](https://ausca.com/skills/document-ocr/SKILL.md), [OpenAPI](https://ausca.com/openapi.yaml), and [catalog](https://ausca.com/catalog.json), then selected `document.ocr` revision `ocr-fixed-r7` (`sha256:024ce18fdfd7d108528b2a4780dbbc5dd9988bcb328a729b7ff2b498697e02b9`). The separate `npx ausca price document.ocr --json` output only gives currency/model/min/max; it omits the revision, policy digest, and schema digests that this audit needs, so I had to read the catalog too.
- **Artifact commit — 2440.964 ms.** I committed a 1200 px JPEG derivative of one genuine scanned public-domain page (232395 bytes) through `POST /v1/artifacts`. The returned `runx:artifact:sha256:26c046818f34e1b63b4e9e3d8a0fc7569bc2b0f4835ed19e5603bf69c48c673c` matched the local digest `sha256:d6d8bad0aff72a74e0454a3ee7bf99c8c48c8e83eeb3570df1e52b24ec6d76eb` and media type `image/jpeg`.
- **Artifact recovery — 8627.324 ms of failed requests.** Before that successful commit, the original 4,456,043-byte PNG—well under the catalog's 10 MiB limit—received HTTP 503 from `POST /v1/artifacts` 3 times under the same commit idempotency key. No payment request had been sent. I used the same scan as a smaller truthful JPEG rather than inventing or changing the document.
- **Challenge — 2208.12 ms.** I sent the exact catalog-bound envelope unsigned to `POST /v1/extract-text`; it returned HTTP 402 with the live x402 v2 requirement. Amount, asset, network, and `payTo` are preserved in `evidence.json` under the challenge observation.
- **Payment — 2774.91 ms.** The official x402 wrapper retried the same request-body digest with `PAYMENT-SIGNATURE`, under a hard `maxPaymentUsd: 0.25` cap. Payer: `0xb86d458bbE799B75de25D6bF7D7A7908D673e242`; transaction: `0xd172d6aab154b725fb1b4185c97bd10daec146dd15691e444b7045dcc169ebb7`.
- **Invocation — 4998.04 ms.** The signed request returned HTTP 202, body status `accepted`, invocation `paid_79c71adf-c524-4598-b53c-e6c1b56a72aa`, and terminal state `succeeded`. The envelope preserved `ausca-ocr-5ad2b47f-1241-4026-b551-5e969d8c4a6c`, input schema `sha256:aa86a0fc7e01438f3f99dc35550f4c3d710795e4302e489de32eae7e403ffd5d`, and output schema `sha256:d9f05704955fca53ed5813e2522f56d4b9fb95e072f84771a369f780b77741d6`.
- **Wait — 13547.498 ms.** The invoke response required 6 explicit readback poll(s); I read GET /v1/invocations/{invocation_id} until terminal.
- **Result — 0.381 ms.** I retrieved the result via inline invocation.output, checked every field against `ausca.document_ocr.output.v1`, and confirmed `source_digest === sha256:d6d8bad0aff72a74e0454a3ee7bf99c8c48c8e83eeb3570df1e52b24ec6d76eb`. Server `output_digest`: `sha256:fcc9e0f16aee5396f6643d8ff38a0c132a931cf09d3773cce3cd97dda9efc2af`; 23 OCR lines and 999 text characters were returned.
- **Receipt — 2,469.936 ms across the page and machine claim.** I fetched [the public Ausca receipt](https://runx.ai/r/d1ff52587e35f061a9b77bfc2f4a4b8f3cb5a54a3fedb0779bf2d1bf8607a040) in 458.85 ms (HTTP 200), then fetched its public-claim JSON from `https://api.runx.ai/v1/receipts/notarizations/d1ff52587e35f061a9b77bfc2f4a4b8f3cb5a54a3fedb0779bf2d1bf8607a040` in 2,011.086 ms (HTTP 200). The claim names publisher `ausca`, venue `Ausca`, subject `document.ocr`, amount `25` cents `USD`, occurrence `2026-09-19T21:24:19.565Z`, and verification `2026-09-19T21:24:20.343Z`—inside this report's timeline.
- **Total — 21745.691 ms.** This covers discovery through independent receipt fetch; the paid invocation itself used one caller-owned idempotency key and was not repeated as a new purchase.
- **Issue 1 (artifact ingress, `POST /v1/artifacts`).** A 4,456,043-byte PNG under the advertised 10 MiB limit returned HTTP 503 `{"code":"unavailable"}` 3 times. The 232,395-byte JPEG derivative succeeded. Return a specific size/processing refusal when applicable, or make ingress reliably honor the published 10 MiB limit.
- **Issue 2 (challenge, SDK `InvocationResult`).** `AuscaClient.invoke` deliberately handles the 402 internally but returns only `result`, `payment`, and `status`; it does not expose the received x402 requirement or unsigned/signed phase timings. I had to wrap `fetch` to report exact `amount`, `asset`, `network`, `payTo`, and the two elapsed times. Add an optional structured trace hook that redacts `PAYMENT-SIGNATURE` by default.
- **Issue 3 (discovery, `npx ausca price document.ocr --json`).** The price command omits `policy_digest`, `offer_revision_digest`, `input_schema_digest`, and `output_schema_digest`. For an evidence-heavy integration, this forces a second catalog read; add these immutable identifiers to the JSON price output.
- **Issue 4 (receipt page, alternate JSON link).** The receipt HTML advertises `https://runx.ai/v1/receipts/notarizations/d1ff...` in `<link rel="alternate" type="application/json">`, but that URL returns HTTP 404. The same path on `https://api.runx.ai` returns the required public claim with publisher, subject, amount, and timestamps. Point the alternate link at the API host so agents can discover the machine record directly.
- **Concrete change.** Add a safe `trace` option shared by CLI and SDK that emits catalog bindings, redacted 402 terms, request-body digest equality, per-phase elapsed milliseconds, verified artifact size, settlement transaction, invocation ID/state, and receipt URL as one JSON record. That directly removes the custom instrumentation required at the challenge and evidence steps.

Secrets and document bytes are not published. All commands, identifiers, timings, and responses above came from this invocation; sensitive headers were replaced with placeholders.
